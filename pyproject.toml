[project]
name = "oil-price-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "python-dotenv",
    "supabase",
    "httpx",
]

[tool.vercel]
entrypoint = "api.webhook:handler"